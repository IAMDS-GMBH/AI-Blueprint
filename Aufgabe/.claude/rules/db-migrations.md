---
description: Regeln fuer Datenbankmigrationen und SQL – automatisch aktiv bei .sql Dateien
globs: ["**/*.sql", "**/migrations/**"]
alwaysApply: false
---

# Datenbank – Pflichtregeln

## Flyway Migration Naming
```
V001__create_customers_table.sql
V002__add_orders_table.sql
V003__add_index_orders_customer_id.sql
```
- `V` = Versionsmigration, `R` = Wiederholbare Migration
- Keine bestehende Migration editieren – immer neue Datei
- Jede Migration muss einzeln rueckrollbar sein (Kommentar mit Rollback-SQL)

## Oracle Konventionen
```sql
-- Tabellennamen: UPPER_CASE, Plural
CREATE TABLE CUSTOMER_ORDERS (
    ORDER_ID    NUMBER(19) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CUSTOMER_ID NUMBER(19) NOT NULL,
    CREATED_AT  TIMESTAMP DEFAULT SYSTIMESTAMP NOT NULL,
    CONSTRAINT FK_ORDER_CUSTOMER FOREIGN KEY (CUSTOMER_ID) REFERENCES CUSTOMERS(CUSTOMER_ID)
);
```

## PostgreSQL Konventionen
```sql
-- snake_case ueberall
CREATE TABLE customer_orders (
    order_id    BIGSERIAL PRIMARY KEY,
    customer_id BIGINT NOT NULL REFERENCES customers(customer_id),
    created_at  TIMESTAMPTZ DEFAULT NOW() NOT NULL
);
```

## Verbote
- **Kein** `SELECT *` in Produktion
- **Kein** DDL ohne Flyway-Migration (niemals manuelles ALTER TABLE)
- **Keine** Stored Procedures fuer Business-Logik (gehoert in den Service-Layer)
- **Kein** `DROP` ohne explizite Benutzer-Bestaetigung
- **Keine** Migration ohne Index auf Foreign Keys
