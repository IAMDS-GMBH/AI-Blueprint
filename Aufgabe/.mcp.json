{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "description": "Browser-Automatisierung und E2E-Testing – Agent kann selbst im Browser verifizieren"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub Integration – PRs, Issues, Repos direkt aus dem Agent heraus"
    },
    "figma": {
      "command": "npx",
      "args": ["-y", "figma-mcp-server"],
      "env": {
        "FIGMA_TOKEN": "${FIGMA_TOKEN}"
      },
      "description": "Design-to-Code Pipeline – Figma Designs direkt in Vue-Komponenten umwandeln"
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres"],
      "env": {
        "POSTGRES_CONNECTION_STRING": "${POSTGRES_CONNECTION_STRING}"
      },
      "description": "PostgreSQL Direktzugriff fuer Queries, Schema-Analyse und Datenpruefung"
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Aktuelle Library-Dokumentation direkt in den Kontext laden – kein veraltetes Halluzinieren"
    }
  }
}
